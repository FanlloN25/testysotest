# 🚀 Запуск веб-сервера для VibeCord

## ❌ ПРОБЛЕМА:
```
Failed to load stylesheet: file:///C:/.../style.css
```
Это происходит потому, что HTML файлы открываются напрямую из файловой системы.

## ✅ РЕШЕНИЕ: Запустите локальный веб-сервер

### Способ 1: Python (рекомендуется)
Откройте командную строку в папке проекта и выполните:
```bash
python -m http.server 8080
```
Затем откройте: http://localhost:8080

### Способ 2: Node.js serve
```bash
npx serve .
```
Затем откройте URL, который покажет команда

### Способ 3: VS Code Live Server
1. Установите расширение "Live Server" в VS Code
2. Щелкните правой кнопкой по index.html
3. Выберите "Open with Live Server"

### Способ 4: Node.js http-server
```bash
npx http-server
```

## 🔄 ДЛЯ РАЗРАБОТКИ FIREBASE:

В отдельном терминале запустите API сервер:
```bash
npm start
```
Это запустит Express сервер на порту 3000 с Firebase интеграцией.

## 📋 ПРАВИЛЬНАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ:

1. **Запустите веб-сервер** (один из способов выше)
2. **Запустите API сервер**: `npm start`
3. **Откройте сайт**: http://localhost:8080

## 🧪 ТЕСТИРОВАНИЕ:
1. Откройте сайт в браузере
2. Нажмите "Регистрация"
3. Заполните форму
4. Проверьте Firebase Console → Authentication → Users

## ⚠️ ВАЖНО:
- Никогда не открывайте HTML файлы напрямую из файловой системы
- Всегда используйте веб-сервер для разработки