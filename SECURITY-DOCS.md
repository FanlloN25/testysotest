# 🔐 VibeCord Secure Authentication System

Продвинутая система аутентификации и управления аккаунтами с многоуровневой защитой для современных веб-приложений.

## 🚀 Возможности системы

### Основные функции
- ✅ **JWT аутентификация** с refresh токенами
- ✅ **Двухфакторная аутентификация (2FA)** с TOTP
- ✅ **Продвинутое управление сессиями**
- ✅ **Rate limiting** и защита от брутфорса
- ✅ **Санитизация данных** и защита от injection
- ✅ **Логирование безопасности** и мониторинг
- ✅ **Ролевая система доступа**
- ✅ **Автоматическая блокировка подозрительной активности**

### Уровни защиты
- 🛡️ **Уровень приложения**: Express.js с Helmet.js
- 🛡️ **Уровень API**: JWT токены и middleware аутентификации  
- 🛡️ **Уровень базы данных**: Firebase Security Rules
- 🛡️ **Уровень инфраструктуры**: Docker, Nginx, Fail2Ban
- 🛡️ **Уровень мониторинга**: Prometheus, Grafana

## 📋 Требования

### Системные требования
- Node.js >= 18.0.0
- npm >= 9.0.0
- Docker & Docker Compose (для production)
- Firebase проект с Firestore

### Зависимости
- Firebase Admin SDK
- Express.js
- JWT (jsonwebtoken)
- Bcrypt
- Speakeasy (2FA)
- QRCode

## 🛠️ Установка и настройка

### 1. Клонирование репозитория
```bash
git clone https://github.com/your-org/vibecord-secure-auth.git
cd vibecord-secure-auth
```

### 2. Установка зависимостей
```bash
npm install
```

### 3. Настройка переменных окружения
```bash
cp .env.example .env
# Отредактируйте .env файл с вашими данными
```

### 4. Firebase настройка

#### Создание проекта Firebase
1. Перейдите на [Firebase Console](https://console.firebase.google.com)
2. Создайте новый проект
3. Включите Authentication и Firestore Database
4. Создайте Service Account Key:
   ```bash
   # Перейдите в Project Settings > Service Accounts
   # Нажмите "Generate new private key"
   # Сохраните как config/firebase-service-account.json
   ```

#### Настройка Security Rules
Скопируйте содержимое файла `firebase.rules` в Firebase Console:
1. Перейдите в Firestore Database > Rules
2. Вставьте правила из `firebase.rules`
3. Опубликуйте правила

### 5. Запуск в development
```bash
npm run dev
```

### 6. Запуск в production с Docker
```bash
# Сборка и запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f auth-api

# Остановка
docker-compose down
```

## 🔧 Конфигурация

### Основные параметры (.env)
```bash
# Firebase
FIREBASE_DATABASE_URL=https://your-project.firebaseio.com
FIREBASE_CLIENT_EMAIL=your-service@project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n..."

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Безопасность
TWO_FACTOR_ENABLED=true
ENABLE_EMAIL_VERIFICATION=true
MAX_LOGIN_ATTEMPTS=5
LOGIN_LOCKOUT_TIME=900000

# CORS
ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
```

### Дополнительные настройки
- `SESSION_TIMEOUT`: Время жизни сессии (мс)
- `MAX_ACTIVE_SESSIONS`: Максимум активных сессий на пользователя
- `RATE_LIMIT_MAX_REQUESTS`: Лимит запросов за 15 минут
- `ENABLE_SUSPICIOUS_LOG_DETECTION`: Включение мониторинга

## 📚 API Документация

### Эндпоинты аутентификации

#### POST /api/auth/register
Регистрация нового пользователя
```json
{
  "email": "user@example.com",
  "username": "username",
  "password": "SecurePass123!"
}
```
**Ответ:**
```json
{
  "message": "Регистрация успешна",
  "data": {
    "success": true,
    "user": {
      "id": "user123",
      "email": "user@example.com", 
      "username": "username",
      "twoFactorEnabled": false
    },
    "tokens": {
      "accessToken": "jwt-access-token",
      "refreshToken": "jwt-refresh-token"
    }
  }
}
```

#### POST /api/auth/login
Вход пользователя
```json
{
  "email": "user@example.com",
  "password": "SecurePass123!",
  "twoFactorCode": "123456" // опционально
}
```

#### POST /api/auth/refresh
Обновление токена
```json
{
  "refreshToken": "jwt-refresh-token"
}
```

#### POST /api/auth/logout
Выход пользователя (требует аутентификации)

### Эндпоинты управления аккаунтом

#### GET /api/account/profile
Получение профиля пользователя (требует аутентификацию)

#### PUT /api/account/profile
Обновление профиля (требует аутентификацию)
```json
{
  "username": "new_username",
  "displayName": "Новое имя",
  "bio": "Моя биография"
}
```

#### PUT /api/account/password
Смена пароля (требует аутентификацию)
```json
{
  "currentPassword": "oldPassword123!",
  "newPassword": "newSecurePass456!"
}
```

### 2FA эндпоинты

#### POST /api/account/2fa/setup
Получение QR кода для настройки 2FA (требует аутентификацию)

#### POST /api/account/2fa/enable
Активация 2FA (требует аутентификацию)
```json
{
  "verificationCode": "123456"
}
```

#### POST /api/account/2fa/disable
Отключение 2FA (требует аутентификацию)
```json
{
  "verificationCode": "123456"
}
```

### Административные эндпоинты

#### GET /api/admin/users
Получение списка пользователей (требует роль admin)

#### GET /api/admin/security-logs
Получение логов безопасности (требует роль admin)

### Системные эндпоинты

#### GET /api/system/status
Статус системы (публичный)

#### GET /api/health
Health check (публичный)

## 🔒 Меры безопасности

### Реализованные защиты

#### 1. Аутентификация
- ✅ Bcrypt хеширование паролей (12 раундов)
- ✅ JWT токены с коротким временем жизни
- ✅ Refresh токены для автоматического обновления
- ✅ Сравнение хешей с защитой от timing атак

#### 2. Авторизация
- ✅ Ролевая система доступа (user, admin, super_admin)
- ✅ Проверка разрешений для каждого эндпоинта
- ✅ Защищенные маршруты с middleware

#### 3. Rate Limiting
- ✅ Лимит попыток входа: 5 за 15 минут
- ✅ Лимит регистрации: 3 за час  
- ✅ Общий rate limiting: 100 запросов за 15 минут
- ✅ Блокировка IP при превышении лимитов

#### 4. Input Validation
- ✅ Валидация email форматов
- ✅ Проверка сложности паролей
- ✅ Санитизация всех входящих данных
- ✅ Защита от XSS и injection атак

#### 5. Session Management
- ✅ Управление активными сессиями
- ✅ Автоматическая очистка истекших сессий
- ✅ Ограничение количества сессий на пользователя
- ✅ Логирование входа/выхода

#### 6. 2FA (Two-Factor Authentication)
- ✅ TOTP алгоритм (Google Authenticator совместимость)
- ✅ QR коды для легкой настройки
- ✅ Обязательная верификация при включении
- ✅ Защита от brute force 2FA кодов

#### 7. Мониторинг и логирование
- ✅ Логирование всех попыток входа
- ✅ Отслеживание подозрительной активности
- ✅ Уведомления о критических событиях
- ✅ Ротация и архивирование логов

### Firebase Security Rules

#### Защита данных
- ✅ Пользователи могут читать/писать только свои данные
- ✅ Строгая валидация структуры документов
- ✅ Запрет массового чтения пользователей
- ✅ Ограничения на изменение критических полей

#### Rate Limiting на уровне БД
- ✅ Проверка частоты операций
- ✅ Защита от массовых запросов
- ✅ Автоматическая очистка старых данных

## 🚀 Развертывание

### Development
```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Тесты
npm test
```

### Production с Docker
```bash
# Клонирование репозитория
git clone <repository>
cd vibecord-secure-auth

# Настройка environment
cp .env.example .env
# Отредактируйте .env файл

# Запуск всех сервисов
docker-compose up -d

# Проверка статуса
docker-compose ps
```

### Production без Docker
```bash
# Установка зависимостей
npm install --only=production

# Запуск
NODE_ENV=production npm start

# Мониторинг с PM2
npm install -g pm2
pm2 start account-api.js --name vibecord-auth
pm2 startup
pm2 save
```

## 📊 Мониторинг

### Метрики системы
- **Аутентификация**: успешные/неуспешные попытки входа
- **Производительность**: время ответа API
- **Безопасность**: заблокированные IP, подозрительная активность
- **Система**: использование памяти, CPU, диска

### Инструменты мониторинга
- **Prometheus**: сбор метрик
- **Grafana**: визуализация дашбордов
- **Fail2Ban**: автоматическая блокировка
- **ELK Stack**: анализ логов

### Настройка дашбордов Grafana
1. Импортируйте преднастроенные дашборды из папки `grafana/`
2. Настройте источники данных (Prometheus)
3. Создайте алерты для критических событий

## 🔧 Кастомизация

### Настройка 2FA
```javascript
// config.js
security: {
  twoFactor: {
    enabled: true,
    issuer: 'YourAppName' // Измените название приложения
  }
}
```

### Кастомизация правил безопасности
```javascript
// firebase.rules - добавьте новые проверки
function isFeatureEnabled(featureName) {
  return request.auth != null && 
    request.auth.token.features != null &&
    request.auth.token.features[featureName] == true;
}
```

### Добавление новых ролей
```javascript
// В обновлении пользователя
const updates = {
  roles: ['user', 'premium'] // Добавьте новую роль
};
```

## 🛠️ Устранение неисправностей

### Частые проблемы

#### 1. Firebase подключение
```bash
# Проверьте переменные окружения
echo $FIREBASE_DATABASE_URL

# Проверьте права доступа к Service Account Key
ls -la config/firebase-service-account.json
```

#### 2. JWT токены
```bash
# Проверьте секретный ключ
echo $JWT_SECRET

# Убедитесь, что ключ достаточно длинный (минимум 32 символа)
```

#### 3. Rate Limiting
```bash
# Проверьте настройки в .env
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
```

#### 4. 2FA проблемы
- Убедитесь, что время на сервере и устройстве синхронизированы
- Проверьте, что код не истек (30 секунд окно)
- Перегенерируйте секрет если проблемы продолжаются

### Логи и отладка
```bash
# Просмотр логов Docker
docker-compose logs -f auth-api

# Включение debug режима
DEBUG_AUTH=true npm run dev

# Мониторинг системы
curl http://localhost:3000/api/system/status
```

## 🔄 Обновление системы

### Обновление зависимостей
```bash
# Проверка устаревших пакетов
npm outdated

# Обновление безопасности
npm audit fix

# Полное обновление (осторожно!)
npm update
```

### Миграция данных
```bash
# Создание миграции
node scripts/create-migration.js

# Выполнение миграций
node scripts/migrate.js
```

### Backup и восстановление
```bash
# Создание backup
./scripts/backup.sh

# Восстановление из backup
./scripts/restore.sh backup-YYYY-MM-DD.tar.gz
```

## 📞 Поддержка

### Контакты
- **Техническая поддержка**: security@vibecord.com
- **Баг-репорты**: [GitHub Issues](https://github.com/your-org/vibecord-secure-auth/issues)
- **Документация**: [Wiki](https://github.com/your-org/vibecord-secure-auth/wiki)

### Сообщество
- **Telegram**: [@VibeCordSecurity](https://t.me/VibeCordSecurity)
- **Discord**: [VibeCord Community](https://discord.gg/vibecord)
- **Reddit**: r/VibeCord

---

**© 2024 VibeCord Security Team. Все права защищены.**

> ⚠️ **Важно**: Эта система разработана для обеспечения максимальной безопасности. Обязательно регулярно обновляйте зависимости и следите за новыми угрозами безопасности.